import * as THREE from 'three';
export class MenuScene{
  constructor(canvas){this.canvas=canvas;this.running=false;if(!canvas)return;this.renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});this.renderer.setPixelRatio(Math.min(1.5,devicePixelRatio||1));this.scene=new THREE.Scene();this.camera=new THREE.PerspectiveCamera(45,1,.1,100);this.camera.position.set(7,4,8);this.camera.lookAt(0,1,0);this.scene.add(new THREE.HemisphereLight(0xffe6a8,0x102410,2));this.train=new THREE.Group();const body=new THREE.Mesh(new THREE.BoxGeometry(4,1.5,1.7),new THREE.MeshStandardMaterial({color:0xa64120,metalness:.35}));body.position.y=1;this.train.add(body);const chimney=new THREE.Mesh(new THREE.CylinderGeometry(.25,.4,1.3,12),new THREE.MeshStandardMaterial({color:0x252525}));chimney.position.set(1.1,2,0);this.train.add(chimney);for(const x of [-1.25,0,1.25])for(const z of [-.82,.82]){const wheel=new THREE.Mesh(new THREE.CylinderGeometry(.42,.42,.18,16),new THREE.MeshStandardMaterial({color:0x171717}));wheel.rotation.x=Math.PI/2;wheel.position.set(x,.35,z);this.train.add(wheel)}this.scene.add(this.train);const stars=new Float32Array(270);for(let i=0;i<90;i++){stars[i*3]=(Math.random()-.5)*20;stars[i*3+1]=Math.random()*8;stars[i*3+2]=(Math.random()-.5)*12}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.BufferAttribute(stars,3));this.particles=new THREE.Points(geo,new THREE.PointsMaterial({color:0xeac968,size:.06}));this.scene.add(this.particles);this.resize();this.running=true;this.loop()}
  resize(){if(!this.renderer)return;const width=this.canvas.clientWidth||innerWidth,height=this.canvas.clientHeight||innerHeight;this.renderer.setSize(width,height,false);this.camera.aspect=width/height;this.camera.updateProjectionMatrix()}
  loop(){if(!this.running)return;requestAnimationFrame(()=>this.loop());this.train.rotation.y=Math.sin(performance.now()/3000)*.08;this.particles.rotation.y+=.0008;this.renderer.render(this.scene,this.camera)}
  stop(){this.running=false}
}
