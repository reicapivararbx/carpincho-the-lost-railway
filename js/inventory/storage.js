export class Storage{constructor(cap=200){this.cap=cap;this.items=new Map()}count(id){return this.items.get(id)||0}deposit(id,amount,inventory){if(amount<=0||[...this.items.values()].reduce((a,b)=>a+b,0)+amount>this.cap||!inventory.has(id,amount))return false;inventory.remove(id,amount);this.items.set(id,this.count(id)+amount);return true}withdraw(id,amount,inventory){if(this.count(id)<amount||!inventory.canAdd(id,amount))return false;this.items.set(id,this.count(id)-amount);if(!this.count(id))this.items.delete(id);inventory.add(id,amount);return true}toJSON(){return {cap:this.cap,items:[...this.items]}}}
